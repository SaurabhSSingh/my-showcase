version: 0.1

phase:
  pre-build:
    comands:
      - npm install
      - npm test
  build:
    comands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
    - dist/bundle.js
